class ItemModel {
  final String title;
  final String subTitle;
  final String trailing;

  const ItemModel({
    required this.title,
    required this.subTitle,
    required this.trailing,
  });
}
